[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push --accept-data-loss --skip-generate; echo 'Starting Node on PORT='$PORT; node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
